# Pedir al usuario un número entero positivo
n = int(input("Introduce un número entero positivo: "))

# Inicializar una lista para los números impares
impares = []

# Recorrer los números desde 1 hasta n
for i in range(1, n + 1):
    if i % 2 != 0:  # Verificar si el número es impar
        impares.append(i)

# Mostrar los números impares separados por comas
print(", ".join(map(str, impares)))
